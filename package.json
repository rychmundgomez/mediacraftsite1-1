{
  "name": "mediacraft-site",
  "version": "1.0.0",
  "description": "MediaCraft single-page site — build script to minify CSS/JS",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "start": "npx serve ."
  },
  "author": "rychmundgomez",
  "license": "MIT",
  "devDependencies": {
    "csso": "^5.0.0",
    "esbuild": "^0.18.0"
  }
}
